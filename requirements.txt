# ASTrace AI – pinned dependencies
# Regenerate with: pip freeze > requirements.txt

# libclang Python bindings for AST parsing
clang==14.0

# OpenAI SDK for structured LLM completions
openai==2.30.0

# Google Gemini SDK (replaces deprecated google-generativeai)
google-genai==1.69.0

# Schema validation and structured LLM output enforcement
pydantic==2.12.0

# Terminal UI rendering
rich==13.7.1

# Loads .env into os.environ at runtime
python-dotenv==1.0.1

# Token counting for context efficiency metrics
tiktoken==0.12.0
